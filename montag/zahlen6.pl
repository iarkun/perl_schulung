#! /usr/bin/perl

use strict;
use warnings;

my $zahl=17;
my $wert=0;
die "Wert darf nicht 0 sein !\n" unless $wert;
#Programm mit Fehler beenden, es sei denn $wert entspricht logisch True
#Kann also nicht 0 sein
 
print $zahl / $wert # if $wert;

